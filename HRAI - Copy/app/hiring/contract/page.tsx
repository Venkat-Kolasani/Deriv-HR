import OfferLetterClient from "./OfferLetterClient";

export const metadata = {
    title: "derivHR — Offer Letter",
};

export default function OfferLetterPage() {
    return <OfferLetterClient />;
}
